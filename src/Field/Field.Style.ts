import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  field: {
    width: 20,
    height: 20,
    borderColor: '#000',
    borderWidth: 1
  },
  centerText: {
    textAlign: 'center'
  }
});
